import { FiltriranjePipe } from './filtriranje.pipe';

describe('FiltriranjePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltriranjePipe();
    expect(pipe).toBeTruthy();
  });
});
